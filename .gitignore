# Rust build artifacts
/target
**/*.rs.bk

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment and secrets
.env
.env.local
*.pem
*.key
secrets/

# Local development
local.toml
*.local.json

# Generated test data
test-data/
coverage/

# Provisioner runtime config (real one is local, not committed)
crates/imogo-provisioner/provisioner.toml

# faq-bot runtime config (real one is local, not committed)
bots/faq-bot/faq-bot.toml

# moderation-bot runtime config and database (local, not committed)
bots/moderation-bot/mod-bot.toml
bots/moderation-bot/data/moderation.db
bots/moderation-bot/data/moderation.db-shm
bots/moderation-bot/data/moderation.db-wal

# Provisioner runtime database (real one is local, not committed)
crates/imogo-provisioner/imogo-provisioner.db
crates/imogo-provisioner/imogo-provisioner.db-shm
crates/imogo-provisioner/imogo-provisioner.db-wal

# Test artifacts
**/*.db-journal
